People
- empty sections -> no users in range tab
- custom cells (web image)
X sections (movie, likes, etc) (Top 3, and see all)
- message to navigation (bar button)
- one view that handles all table views (variable toggle)

Map
- reintroduce pins
- clustering of pin (k-nearest neighbors) (recalc on zoom level)
- single user takes you to their profile page, otherwise you see a table view of users at pin (two custom annotations)

